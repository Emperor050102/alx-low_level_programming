#include <main.h>

/*we would use the manin to input variables
 * for would be used to assign the parameters
 * and we would use _putcher to print to screen
 * return 0
 */

int main(void) {
	int i;
	for(i=o, i<5, i++) {
		_putcher("%d\n" , i);
	}
	return 0;
}
